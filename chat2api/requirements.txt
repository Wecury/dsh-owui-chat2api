# chat2api runtime dependencies. `tiktoken` is OPTIONAL: when present it is
# used to estimate token counts for upstreams that do not return a `usage`
# block; when absent, those calls are recorded with estimated=true and zeros.
requests>=2.31
playwright>=1.40
tiktoken>=0.5  ; python_version >= "3.10"
